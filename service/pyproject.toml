[tool.uv.workspace]
members = ["packages/libs/*", "packages/apps/*"]

[tool.uv]
dev-dependencies = [
    "pytest>=8,<10",
    "pytest-asyncio>=0.24,<2",
    "ruff>=0.8,<1",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["packages"]
